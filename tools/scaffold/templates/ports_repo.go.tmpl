package ports

import (
	"context"

	"{{.DomainImport}}"
)

// Repository is the outbound driven port interface for {{.ModuleName}} data storage.
type Repository interface {
	Save(ctx context.Context, item *domain.{{.TypeName}}) error
	FindAll(ctx context.Context) ([]domain.{{.TypeName}}, error)
}
